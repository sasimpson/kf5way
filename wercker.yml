box: debian
build:
    steps: 
        - arjen/hugo-build@1.14.1
        - script:
            name: ls stuff
            code: |
                ls -haltr /pipeline/source/public
deploy:
    steps:
        - script:
            name: ls output
            code: |
                ls -haltr /pipeline/output
        - script:
            name: ls source
            code: |
                ls -haltr /pipeline/source
        - sasimpson/rackspace-cloudfiles-swiftly@0.0.1:
            path: public
            region: DFW