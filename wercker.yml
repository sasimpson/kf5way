box: debian
build:
    steps: 
        - arjen/hugo-build@1.14.1
        
deploy:
    box: python
    steps:
        - sasimpson/rackspace-cloudfiles-swiftly@0.0.2
        - script:
            name: python version
            code: |
                echo "python version $(python --version) running"
                echo "pip version $(pip --version) running"